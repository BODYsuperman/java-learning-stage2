# ==================== 1. Java编译核心产物（必忽略）====================
# 字节码文件（所有.class）
*.class
# 项目编译输出目录（Maven/Gradle/IDEA默认）
target/
build/
out/
# 临时编译文件
*.tmp
*.bak

# ==================== 2. IDE配置文件（IDEA+Eclipse，必忽略）====================
# IntelliJ IDEA（你常用的IDE，核心忽略）
.idea/
*.iml
*.iws
*.ipr
# Eclipse
.settings/
.project
.classpath
.metadata/
bin/

# ==================== 3. 依赖包/缓存（必忽略）====================
# Maven本地缓存（无需提交）
.m2/
# Gradle缓存
.gradle/
gradle-wrapper.jar
# 本地打包的jar/war（若不是发布产物，可忽略）
*.jar
*.war
*.ear
# 第三方依赖包（如手动导入的lib目录，若用Maven/Gradle则必忽略）
lib/

# ==================== 4. 系统/日志临时文件（通用忽略）====================
# Mac系统隐藏文件
.DS_Store
# Windows系统文件
Thumbs.db
desktop.ini
# 日志文件（所有环境的日志）
*.log
logs/
# 临时文件
*.tmp
*.swp
*.swo
# 环境配置文件（本地配置，如数据库密码，必忽略）
*.properties
*.yml
*.yaml
# 注意：若有通用配置（如application.yml），需写成 !application.yml 保留，下面有说明

# ==================== 5. 版本控制无关文件（可选忽略）====================
# 测试报告
test-report/
coverage/
# 打包/脚本文件
*.sh
*.bat
!start.sh # 若有需要提交的脚本，用!排除